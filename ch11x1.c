/* Source file for ch11x1
 *
 * Bitwise AND
 *  use on binary numbers, then octal numbers, finally hex numbers. First
 *  problem, no easy way to express binary using printf.
 *
 *  Develop a function that takes two  character strings of 1's and 0's,
 *  and outputs the bitwise AND as a character string*
 *
*/

#include <stdio.h>

int main(int argc, char *argv[]){

    char result[17] = {"Missing function"};
    printf("Bitwise AND\n");
    printf("%s\n", argv[1]);
    printf("%s\n", argv[2]);
    printf("%s\n", result);





    return(0);
}
